# Discord Bot Configuration
    DISCORD_TOKEN=seu_token_aqui
    CLIENT_ID=id_do_seu_bot_aqui
    GUILD_ID=id_do_servidor_aqui

    # Database (Railway PostgreSQL)
    DATABASE_URL=postgresql://user:password@host:port/database

    # Owner ID (acesso total)
    OWNER_ID=1195254699943796791

    # OpenAI — usado pelo Bryan (I.A.)
    # Gere em: https://platform.openai.com/api-keys
    OPENAI_API_KEY=sk-sua_chave_aqui

    # URL pública do site/dashboard (usada pelo /fut chamar pra colocar um
    # botão "Confirmar no site" no anúncio do Discord). Opcional — se não
    # definir, o comando só não mostra o botão.
    SITE_URL=bryanfut.up.railway.app
